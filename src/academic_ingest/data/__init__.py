"""Package data bundled with `academic_ingest` (loaded via `importlib.resources`).

Contains static snapshot files (e.g. the UW Bellevue College equivalency guide
HTML) that production code needs at runtime with no network access and no
dependency on the current working directory.
"""
